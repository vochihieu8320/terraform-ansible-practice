# inventory.ini — WHICH machines Ansible manages.
# The [app] group holds our EC2. The vars tell Ansible how to SSH in —
# exactly the same connection you used manually:  ssh ubuntu@47.129.88.205

[app]
47.129.88.205

[app:vars]
ansible_user=ubuntu
ansible_ssh_private_key_file=~/.ssh/id_ed25519
