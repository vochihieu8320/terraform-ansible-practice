# ansible.cfg — defaults so we don't repeat flags on every command.
[defaults]
inventory          = inventory.ini
host_key_checking  = False          # don't prompt on first SSH to a new host (lab only)
remote_user        = ubuntu
interpreter_python = /usr/bin/python3
